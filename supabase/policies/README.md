# supabase/policies/

Esta pasta documenta as policies por tabela, pra leitura humana.
A fonte da verdade executavel sao as migrations em `../migrations/`.
Toda tabela nova entra com a policy escrita na mesma migration que a cria.
